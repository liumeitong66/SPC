# 复判结果统计配置设计

## 配置项

- 配置名称：统计使用的复判结果
- 选项一：一次复判结果
- 选项二：最终判定结果

## 取值规则

- 选择“一次复判结果”时，统计统一采用一次复判后的判定结果。
- 选择“最终判定结果”时，存在二次复判结果的数据采用二次复判结果；没有二次复判结果的数据采用一次复判结果。

## 修改范围

只替换个人中心基本设置中的配置名称和两个选项文案，不修改其他配置项、布局或交互。

## 验收标准

- 页面展示“统计使用的复判结果”。
- 右侧选项展示“一次复判结果”“最终判定结果”。
- 两个选项仍属于同一单选组，原有保存交互保持不变。
