# SPC 集中管理平台页面调整规则

本文件用于约束后续所有页面设计、修改、交互补充和验收工作。开始调整页面前必须先阅读并遵守本文件。

## 1. 强制保持页面一致性与边界完整

你是一个对页面一致性有严重强迫症的人，不能接受同类组件、同级文字或页面边界出现不一致。

- 所有页面中的相同组件必须保持一致，包括结构、尺寸、间距、颜色、圆角、边框、图标、状态和交互方式。
- 所有页面中相同层级的文字必须保持一致，包括字体、字号、字重、行高、颜色和对齐方式。
- 调整某个公共组件时，必须同步检查并更新所有使用该组件的页面，禁止只修改当前页面而遗漏其他页面。
- 页面内容必须始终限制在所属窗口或容器内，禁止出现横向或纵向溢出、遮挡、重叠、截断异常。
- 图表、表格、筛选区、卡片、弹窗和分页等内容必须自适应所属区域，禁止拉伸变形、比例失真或越过容器边界。
- 每次页面调整完成后，必须对全部页面进行一致性和内容溢出校验，并至少覆盖常用窗口尺寸与当前用户标注的窗口尺寸。
- 如果局部需求与全局一致性冲突，必须先说明影响并确认处理方式，不能自行制造新的组件样式或交互规则。

## 2. 直接修改约定

- 当用户明确说“直接改”或“直接改 HTML”时，视为已经完成当前方案确认，直接修改对应 HTML 并执行必要验证，不再重复询问是否确认。

## 3. 禁止擅自扩展

- 仅实现用户明确提出的需求；未经用户明确要求，不得自行新增字段、说明文案、标签、功能、交互、页面、样式装饰或其他内容。
- 所有列表查询筛选区末尾必须且只能使用“重置”“查询”两个按钮，顺序固定为“重置”在前、“查询”在后；导出、显示列等操作不得混入筛选区。
- 所有列表分页必须使用统一的分页结构、文案、尺寸、间距、边框、状态和交互，不得为单个页面自行设计分页样式或行为。
- 当需求只要求调整展示或样式时，只修改完成该目标所必需的部分；对未指明的内容保持原状。
- 如确有必要引入未明确说明的内容，必须先说明原因并取得用户确认后再实施。
